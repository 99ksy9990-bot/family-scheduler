# Family Scheduler — 기능·디자인 검토

2026-08-11 · 코드(`src/`) + QA 스크린샷(`audit-2026-08-05/`, `design-qa-*.png`) 기준

---

# 1부. 기능 검토

## 없어서 아쉬운 것 — 가족 캘린더의 기본기

### F1. 일반 일정에 반복이 없음 ★최우선
반복은 **자녀 스케줄에만** 있습니다. `childSchedules`는 요일 반복을 지원하지만, `events`(가족 일정)는 전부 단발성입니다.

- 매주 화요일 부모 모임, 매월 15일 관리비, 격주 대청소, 매년 정기검진 → 전부 수동 재입력
- 이미 `scheduleExceptions`(이 날짜만 건너뛰기)와 `RecurringActionDialog`(이 날짜만/전체 수정)라는 **반복 인프라가 완성돼 있습니다.** 일반 일정에 `repeat: { freq, interval, until }`만 붙이면 기존 UI를 그대로 재사용할 수 있습니다. 투자 대비 효과가 가장 큰 항목.

### F2. 주간 뷰가 없음 ★최우선
현재 캘린더는 **월 그리드 + 선택일 상세**뿐이고, 홈은 **오늘**만 보여줍니다. 그런데 가족 일정에서 실제로 가장 자주 필요한 단위는 "이번 주"입니다 — 누가 언제 아이를 데리러 가는지, 이번 주말에 뭐가 있는지.

`ChildWeekView`라는 이름의 컴포넌트가 있지만 실제로는 월 그리드 + 선택일 상세라 주간 뷰가 아닙니다. 홈에 "이번 주" 스트립 하나만 추가해도 체감이 크게 달라집니다.

### F3. 담당자가 1명뿐
`event.member`가 단일 값입니다. "아빠·엄마 둘 다 참석", "등원은 아빠, 하원은 엄마"를 표현할 수 없습니다. 자녀 일정엔 `pickupBy`가 있는데 일반 일정엔 그것조차 없습니다.

→ `members: []` 배열화 + 일반 일정에도 픽업 담당 필드.

### F4. 충돌을 사후에만 알려줌
`overlappingEventIds()`로 "시간 겹침" 배지를 띄우는 건 좋습니다. 그런데 **등록하는 순간엔 아무 말이 없습니다.** 저장하고 목록을 봐야 알게 됩니다.

가족 앱에서 충돌 감지의 진짜 가치는 "이 시간에 엄마가 이미 다른 일정이 있어요"를 **입력 중에** 말해주는 것입니다. 모달 하단에 인라인 경고 한 줄이면 됩니다.

### F5. 할 일에 반복이 없고, 분류가 3개로 고정
```js
const categories = ['긴급', '장보기', '집안일']   // TasksView, 하드코딩
```
- 분리수거·주간 장보기 같은 반복 할 일을 매번 새로 만들어야 함
- 가족마다 필요한 분류가 다른데 수정 불가 (구성원·근무 유형은 커스터마이즈되는데 할 일 분류만 고정인 게 일관성 없음)

### F6. 캘린더에 "오늘로 이동"이 없음
`‹ 2026년 8월 ›` 화살표만 있어서 몇 달 넘긴 뒤 오늘로 한 번에 못 돌아옵니다. 월 제목을 탭하면 오늘로 가는 정도로 충분.

---

## 이미 있는데 어긋나는 것

### F7. 할 일 개수와 목록이 안 맞음
헤드라인은 `remaining` = **전체** 미완료 개수인데, 아래 목록은 **필터된** 결과입니다. 실제 QA 스크린샷에서 "남은 할 일이 1개 있어요" 아래로 긴급 0 / 집안일 0 / 장보기 0이 나옵니다. 사용자는 할 일이 사라진 줄 압니다.

→ 헤드라인도 필터 기준으로 세거나, "이번 달 0개 · 전체 1개"로 병기.

### F8. 확인 대화상자와 되돌리기가 이중으로 걸림
`window.confirm`이 10곳인데, **Undo 토스트가 이미 전 삭제 경로에 붙어 있습니다.** 확인 → 삭제 → "되돌리기" 토스트는 한 단계 과합니다. 게다가 네이티브 confirm은 앱 디자인과 이질적이고 iOS 스탠드얼론 PWA에서 특히 어색합니다.

→ confirm을 없애고 Undo로 통일. 되돌릴 수 없는 것(전체 반복 삭제, 백업 덮어쓰기)만 자체 다이얼로그 유지.

### F9. 검색이 날짜를 하루씩 순회
`SearchPanel`은 기본 범위 ±365일(=730일)을 **하루씩 루프 돌며** 매일 `eventsForDate()`를 호출하고, 최대 1095일까지 갑니다. 그리고 이 계산이 **검색어 한 글자마다** 다시 돕니다(디바운스 없음).

→ 입력 디바운스 300ms + 단발 일정은 배열 직접 필터, 반복 일정만 날짜 순회.

### F10. 백업 불러오기에 검증·미리보기가 없음
```js
const imported = JSON.parse(await file.text())
applySharedState(imported)   // 즉시 전체 덮어쓰기
```
JSON 파싱만 성공하면 전혀 다른 파일이라도 상태를 통째로 갈아치웁니다. Undo가 붙어 있긴 하지만, "일정 42개 · 할 일 8개를 불러옵니다. 현재 내용은 대체됩니다" 정도의 확인 단계가 필요합니다.

### F11. 알림 설정과 실제 발송의 연결이 불확실
모달에 "30분 전"(일정), "당일/하루 전"(할 일) 옵션이 있고 `reminder` 필드에 저장됩니다. 그런데 이 값이 실제 push 발송(`send-family-push`, cron 기반)으로 이어지는지 코드상 연결이 확인되지 않았습니다. **설정했는데 안 오는 알림**은 앱 신뢰도를 가장 빠르게 깎는 요소입니다.

→ 연결돼 있다면 문제없고, 아니라면 UI에서 임시로 숨기는 편이 낫습니다.

### F12. 근무 코드가 범례 없이 노출
홈 카드에 `● D`, 관리 화면에 "D·E·N·휴를 선택하세요". 처음 쓰는 가족 구성원(특히 근무자 본인이 아닌 사람)에겐 의미가 전달되지 않습니다. → pill에 `D 주간`처럼 병기.

### F13. 기념일은 "이 회차만" 개념이 없음
반복 일정엔 "이 날짜만 취소"가 있는데, 기념일은 홈에서 삭제 시 **전체가 사라집니다**. 매년 반복되는 성격상 같은 선택지가 필요합니다.

---

## 데이터 모델에서 나중에 발목 잡을 것

| 항목 | 현재 | 문제 |
|---|---|---|
| 시간 | `'오전 9:00'` 한글 문자열 | 정렬 때마다 `timeToMinutes()`로 파싱. 타임존·i18n 확장 시 전면 수정 |
| 종일 | `time: '종일'` 문자열 | 값과 플래그가 섞여 있음. `allDay: true`가 안전 |
| 다중일 일정 | `endDate` 존재 | 월 그리드에서 기간 막대로 그려지는지 확인 필요 — 안 그려지면 사실상 시작일 일정 |
| 구성원 삭제 | `member`가 프로필 id 문자열 | 프로필 삭제 시 기존 일정이 orphan. `memberForId` fallback 동작 확인 필요 |

---

# 2부. 디자인 검토

## 잘한 것

크림 배경(#f7f7f2)에 라벤더·세이지·피치 톤을 얹은 팔레트가 따뜻하고 일관됩니다. A2Z 한글 디스플레이 타이포의 존재감이 확실해서 흔한 캘린더 앱과 뚜렷이 구분됩니다. 카드 라운드·그림자·여백 규칙도 화면 전반에 일관되게 적용돼 있고, 일·토요일 색 구분과 한글 카피 톤도 자연스럽습니다. **시각적 완성도 자체는 높습니다.** 아래는 전부 정보 구조 문제입니다.

## 문제

### D1. 모바일 홈 — 앱을 여는 이유가 폴드 아래에 있음 ★최우선
현재 순서: 인사말(대형) → 근무 카드 → 자녀 카드(비어 있어도 카드 하나 통째로) → **오늘의 일정**

QA 스크린샷에서 "오늘의 일정"의 첫 카드가 화면 맨 아래에 겨우 걸칩니다. 사용자가 앱을 여는 이유는 오늘 뭐가 있는지 보려는 것인데, 그게 스크롤해야 보입니다.

→ 인사말을 1줄로 축소, 근무·자녀는 카드 대신 **한 줄 요약 스트립**으로 합치고, 오늘 일정을 첫 화면에 올리기.

### D2. 모바일 캘린더 — 날짜를 눌러도 반응이 없어 보임 ★최우선
월 그리드가 뷰포트를 거의 다 차지해서, 선택일 상세 패널이 **완전히 폴드 아래**입니다. 날짜를 탭하면 시각적으로는 셀 테두리만 바뀌고 아무 일도 안 일어난 것처럼 보입니다.

→ 셀 높이 축소(6행 × 현재 높이는 과함) + 상세 패널 상단이 폴드에 걸치게, 또는 탭 시 바텀시트로 올라오게.

### D3. 빈 카드가 화면을 크게 차지
데스크톱 "자녀 일정" 카드는 "오늘은 수업이 없습니다" 한 줄인데 카드 높이의 3/4가 비어 있습니다(옆 카드와 높이를 맞추느라). 빈 상태는 높이를 줄이거나 접는 게 맞습니다.

### D4. 월 그리드 정보 밀도가 너무 낮음
데스크톱 셀이 100px 넘게 큰데 실제 표시는 9~10px 이벤트 텍스트 2줄 또는 점뿐입니다. 넓은 화면의 이점을 못 쓰고 있습니다.

→ 데스크톱: 이벤트 칩 2~3개 + `+N개`. 모바일: 점 유지하되 셀 높이 축소.

### D5. 모바일에서 공휴일을 알 수 없음
데스크톱은 `광복절` 빨간 텍스트 + 점(중복 표기), 모바일은 **점만** 찍혀서 15일이 광복절인지 그냥 일정이 있는 건지 구분이 안 됩니다.

→ 데스크톱은 점 제거(텍스트로 충분), 모바일은 셀 배경 틴트나 축약 라벨 추가.

### D6. 헤더 아이콘 4개가 전부 라벨 없음
검색 · 사람+체크 · 사람들 · 포커스가 나란히 있는데, 특히 **사람 아이콘 2개는 서로 구분이 안 됩니다.** 툴팁이 있지만 모바일에선 안 보입니다. 이제 하단 내비에 "설정" 탭이 생겼으니:

→ 헤더는 **검색 하나만** 남기고 나머지는 설정 탭으로. 집중 모드는 홈 화면 내 토글로.

### D7. 오늘과 선택일의 표시가 비슷함
둘 다 사각 강조라 층위가 안 잡힙니다. → 오늘은 숫자 색/하단 점, 선택은 채워진 배경 — 식으로 분리.

### D8. 모바일 할 일이 3단 스택
3개 카테고리를 세로로 쌓아서, 비어 있으면 **빈 상태 3개를 스크롤**하게 됩니다. 3열 그리드는 데스크톱 발상입니다.

→ 모바일은 단일 리스트 + 상단 카테고리 칩 필터. (기간 필터와 합쳐 한 줄로)

### D9. 일정 관리 화면이 폼 우선
등록 폼이 목록 **위에** 항상 펼쳐져 있어서, 이미 등록한 항목을 확인하려면 긴 폼을 스크롤로 통과해야 합니다. 일상 사용에선 조회가 등록보다 훨씬 잦습니다.

→ 목록 우선 + `+ 추가` 버튼 → 바텀시트.

### D10. 데스크톱이 모바일 레이아웃의 확대판
1265px 화면에서도 콘텐츠가 중앙 좁은 폭에 몰리고 좌우·하단 여백이 큽니다. 캘린더 화면은 우측 패널 아래가 통째로 비어 있습니다.

→ 데스크톱은 캘린더 + 선택일 상세 + 오늘의 할 일 3열처럼 밀도를 올릴 여지가 큽니다.

### D11. 타이포 스케일에 중간 단계가 없음
eyebrow 12px에서 h1 40px+로 바로 점프합니다. 카드 제목·본문 사이 위계가 약해 보이는 원인.

### D12. 접근성 마감
`role="dialog"` / `aria-modal` / `aria-label`은 꼼꼼히 붙어 있습니다. 다만 **Escape로 닫기, 포커스 트랩, 닫은 뒤 포커스 복귀**가 어느 모달에도 없습니다. 키보드 사용자는 모달에 갇힙니다.

### D13. (확인 필요) 날짜 버그가 화면에 보임
`design-qa-calendar-mobile.png`는 8월 11일 08:05에 촬영됐는데 선택일이 **10일**입니다. 앞선 검토에서 지적한 `const today = new Date()` 모듈 최상단 고정 문제가 실제로 화면에 드러난 것으로 보입니다. (개발 서버를 전날부터 띄워둔 상태였다면 정확히 이렇게 나옵니다.)

---

# 3부. 권장 우선순위

## 1순위 — 체감이 즉시 바뀌는 것
1. **모바일 홈 재구성** — 오늘 일정을 폴드 위로 (D1)
2. **모바일 캘린더 상세 패널** — 폴드 안으로 또는 바텀시트 (D2)
3. **일반 일정 반복 지원** — 기존 반복 인프라 재사용 (F1)
4. **주간 뷰 또는 홈 "이번 주" 스트립** (F2)

## 2순위 — 신뢰를 지키는 것
5. 할 일 개수/목록 불일치 (F7)
6. 알림 설정 ↔ 실제 발송 연결 확인 (F11)
7. 백업 불러오기 검증·미리보기 (F10)
8. 검색 디바운스 + 알고리즘 개선 (F9)
9. confirm 제거하고 Undo로 통일 (F8)

## 3순위 — 완성도
10. 헤더 아이콘 정리 (D6)
11. 담당자 복수 지정 + 픽업 담당 (F3)
12. 입력 중 충돌 경고 (F4)
13. 할 일 분류 커스터마이즈 + 반복 (F5)
14. 모달 Escape·포커스 트랩 (D12)
15. 모바일 할 일 단일 리스트화 (D8)
16. 일정 관리 목록 우선 재배치 (D9)
