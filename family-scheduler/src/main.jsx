import React from "react";
import { createRoot } from "react-dom/client";
import App from "../family_scheduler_v11.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
