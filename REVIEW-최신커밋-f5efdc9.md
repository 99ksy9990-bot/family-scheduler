# Family Scheduler 재검토 — 최신 커밋 `f5efdc9` 기준

2026-08-11 · 코드 + 배포본(family-scheduler-sooty.vercel.app) 동시 확인

> 이 문서가 현재 기준입니다. 앞선 3개 리뷰(`CODE-REVIEW`, `REVIEW-기능-디자인`, `REVIEW-라이브검증`)는 이전 커밋 기준이라 대부분 무효입니다.

**폴더 상태**: `origin/main`과 동일 (`f5efdc9`, 0 ahead / 0 behind). 별도 다운로드 불필요했습니다.
**배포 상태**: 이 커밋이 라이브에 반영돼 있습니다.

---

## 요약

지난 6개 커밋에서 **108개 파일, +7,598 / −8,442줄**이 바뀌었습니다. 제가 지적했던 항목의 **대부분이 해결**됐습니다. 남은 건 아래 6가지뿐이고, 그중 급한 건 없습니다.

`npm run lint` 통과(경고 0). 빌드는 이 샌드박스에서 rolldown 네이티브 바이너리 아키텍처 불일치로 실패하지만 **코드 문제가 아닙니다** — CI가 ubuntu에서 `npm ci → lint → build → e2e`를 돌립니다.

---

## 해결 확인된 항목

### 인프라·성능
| 이전 지적 | 조치 |
|---|---|
| `vite.config.js` 부재 | 신규 작성 — PWA(injectManifest) + rolldown 코드 스플리팅 3그룹 |
| `date-holidays` 전 세계 데이터 번들 | `scripts/generate-korean-holidays.mjs`로 **빌드 타임 생성** → `src/data/korean-holidays.json`. 패키지는 devDependencies로 이동 |
| 폰트 TTF 5MB | woff2 7종 836KB, `font-display` 7개 전부 적용 |
| 서비스워커 캐싱 없음 | `src/sw.js` + vite-plugin-pwa. e2e에 오프라인 테스트까지 존재 |
| 의존성 `"latest"` 8개 | **전부 정확한 버전 고정** |
| 테스트·CI·ErrorBoundary 없음 | Playwright 9개 시나리오 + GitHub Actions + `ErrorBoundary.jsx` |
| Supabase 자격증명 하드코딩 | fallback 제거, 환경변수 필수화 |
| localStorage 디바운스 없음 | `usePersistedValue` 250ms |

### 버그
| 이전 지적 | 조치 |
|---|---|
| `const today = new Date()` 모듈 고정 | `useToday` 훅 — 자정 타이머 + `focus` + `visibilitychange` |
| **L1** 자녀 일정 정렬 안 됨 | `timeToMinutes` 정렬 + 동시간 제목 정렬. 라이브 확인 |
| **L3** 근무만 24시간제 | `formatDisplayTime()` — 라이브에서 "오후 10:30 종료" 확인 |
| **L4** 할 일 개수 불일치 | `현재 범위 N개 · 전체 N개` 병기 |
| **L2** 홈/캘린더 카운트 기준 불일치 | `가족 일정 0개 / 자녀 일정 4개 / 마감 할 일 1개` 로 분리, 각각 클릭 가능 |

### 기능
| 이전 지적 | 조치 |
|---|---|
| **F1** 일반 일정 반복 없음 ★ | `lib/recurrence.js` — 일/주/월/년 + 간격 + 종료일. 월말·윤년 보정까지 처리 |
| **F2** 주간 뷰 없음 ★ | 홈에 "이번 주 일정" 7일 스트립 |
| **F3** 담당자 1명 | `members[]` 배열 + `AvatarGroup` 겹침 표시 |
| **F4** 입력 중 충돌 경고 없음 | 모달에 `form-warning` — 겹치는 일정 제목까지 표기 |
| **F6** "오늘로 이동" 없음 | 월 네비게이션 옆 `오늘` 버튼 |
| **F8** confirm + Undo 이중 | `window.confirm` **0건**. `AppDialogProvider`로 자체 다이얼로그 |
| **F9** 검색 O(일수) 순회 | 300ms 디바운스 + 단발 일정은 배열 직접 필터, 반복/기념일/공휴일만 순회 |
| **F10** 백업 검증 없음 | `lib/backup.js` `inspectBackup()` — 컬렉션별 개수 미리보기 후 확인 |

### 디자인
| 이전 지적 | 조치 |
|---|---|
| **D5** 점·텍스트 중복 표기 | `.day-dots { display: none }` — 텍스트만 유지 |
| **D7** 오늘/선택일 구분 안 됨 | 오늘은 배경 틴트, 선택은 테두리로 분리 |
| **D12** 포커스 트랩 없음 | `useModalAccessibility` — **8개 모달 전부** 적용 |
| **D6** 헤더 아이콘 4개 | 검색·집중 2개로 축소 (이전 커밋에서) |
| 리포 위생 | QA 스크린샷 40여 장 + 구버전 `family-scheduler/` 폴더 삭제 |

---

## 남은 항목

### R1. 할 일에만 반복이 없음
```jsx
{!isTask && <fieldset className="repeat-field">…}   // 일정에만 노출
```
일정은 반복이 되는데 할 일은 안 됩니다. 분리수거·주간 장보기가 여전히 수동입니다. **`recurrence` 로직이 이미 있으니 할 일에도 연결만 하면 됩니다.**

### R2. 할 일 분류 3개 하드코딩
```js
const categories = ['긴급', '장보기', '집안일']
```
구성원·근무 유형·학기 기간은 전부 커스터마이즈되는데 할 일 분류만 고정입니다.

### R3. 기념일은 "이 회차만" 개념이 없음
일정 반복은 `RecurringActionDialog`로 "이 날짜만 / 전체" 선택이 되는데, 기념일 삭제는 `anniversaryId`로 **전체를 지웁니다**. 매년 반복되는 성격상 같은 선택지가 필요합니다.

### R4. 폰트 서브셋 미적용
woff2 전환으로 5MB → 836KB까지 왔지만, **한글 서브셋은 아직입니다.** 상용 2,350자 기준이면 폰트당 30~50KB, 총 200KB대까지 내려갑니다. Light/Medium을 실제로 쓰는지도 확인해 보세요(7종 전부 로드 중).

### R5. `App.jsx` 2,051줄
`SettingsView`만 `views/`로 분리됐고 Home·Calendar·Tasks·Schedules·Modal·Search는 여전히 한 파일입니다. `lib/`·`hooks/` 분리가 잘 돼 있으니 뷰만 옮기면 되는 상태입니다. 급하진 않습니다.

### R6. 시간 데이터 모델
`time: '종일'`이라는 한글 문자열이 값과 플래그를 겸하고, 시간은 `'오후 9:00'` 문자열이라 정렬마다 파싱합니다. 지금 동작엔 문제없지만 타임존·다국어 확장 시 전면 수정 지점입니다.

---

## 새로 발견한 것 (전부 사소함)

### N1. 홈 요약 3개 버튼에 구분자가 없음
```
가족 일정 0개  자녀 일정 4개  마감 할 일 1개
```
셋 다 클릭 가능한 버튼인데 시각적 구분이 공백뿐이라 하나의 문장으로 읽힙니다. `·` 구분자나 칩 스타일 권장.

### N2. 주간 스트립이 하루에 한 종류만 표시
각 칸이 카운트 1줄 + 일정명 1줄입니다. 8/11은 `자녀 4 / 영어`, 8/13은 `가족 1 / 영재원`. **가족과 자녀 일정이 같은 날 있으면 하나만 보입니다.**
덧붙여 8/15 광복절이 `가족 1`로 집계됩니다 — 공휴일은 별도 표기가 자연스럽습니다.

### N3. 이전·다음 달 칸이 클릭은 막혔는데 내용은 보임
8월 그리드 마지막 줄의 9/1·9/3 칸에 "영재원"이 표시됩니다. 해당 셀은 `disabled`라 눌러도 반응이 없어서, 보이는데 못 누르는 상태입니다.

### N4. 가족 모드에서 자녀 일정 안내가 없음
8/11에 자녀 일정이 4개 있는데 가족 모드 상세는 "등록된 일정이 없습니다"입니다. 홈에서 카운트를 분리한 건 좋은 개선이지만, 여기서도 "자녀 일정 4개 → 자녀 탭에서 보기" 한 줄이 있으면 완결됩니다.

### N5. 빈 카드 높이 (기존 D3 잔존)
근무 카드가 옆 자녀 카드와 높이를 맞추느라 하단 1/3이 빕니다. 데스크톱 캘린더 우측 패널 아래 여백도 그대로입니다(D10).

---

## 확인하지 못한 것

**모바일 레이아웃(D1 홈 폴드 / D2 캘린더 상세 패널 / D8 할 일 3단 스택)은 이번에도 검증 못 했습니다.** 브라우저 창을 430px로 줄여도 뷰포트가 1568px에서 바뀌지 않았습니다.

다만 `tests/app.spec.js`에 모바일 시나리오가 이미 들어가 있습니다:
- `모바일에서도 주요 내비게이션이 보인다`
- `모바일 홈 카드 간격이 같고 가로로 넘치지 않는다`

**`npm run test:e2e`를 로컬에서 돌려 보시고**, 폴드 위치는 실기기나 개발자도구 디바이스 모드로 직접 확인해 주세요.

---

## 권장 순서

1. **R1** 할 일 반복 — 기존 `recurrence` 재사용, 가장 저렴한 기능 추가
2. **N1~N4** 마감 다듬기 — 각각 몇 줄
3. **R2** 할 일 분류 커스터마이즈
4. **R3** 기념일 회차 단위 관리
5. **R4** 폰트 한글 서브셋 — 콜드 스타트 개선
6. **R5** `App.jsx` 뷰 분리 — 여유 있을 때

지난 검토 대비 개선 폭이 큽니다. 특히 반복 일정과 주간 뷰가 들어가면서 "가족 캘린더"로서의 기본기가 채워졌고, 테스트·CI·ErrorBoundary로 안전망도 생겼습니다.
