# 라이브 배포본 검증 — family-scheduler-sooty.vercel.app

2026-08-11 검토 · 실제 데이터가 들어 있는 배포본을 직접 열어 확인

> **중요 정정**: 앞선 두 리뷰는 로컬 `stitch_` 폴더 기준이었는데, **배포본이 로컬 소스보다 훨씬 앞서 있습니다.** 이전 리뷰의 P0 항목 상당수가 이미 해결돼 있었습니다. 아래에 실측으로 확인한 내용을 정리합니다.

---

## 1. 이미 해결된 항목 (앞선 리뷰에서 철회)

| 이전 지적 | 배포본 실측 | 판정 |
|---|---|---|
| **P0-1** JS 번들 1.9MB 단일 청크 / `date-holidays` | 총 **658KB**, 7개 청크로 분리<br>`index 235KB` · `react-vendor 185KB` · `supabase-vendor 202KB` · `icons-vendor 11KB` + 패널 3개 지연 로드 | ✅ 해결 |
| **P0-1** `date-holidays` 전 세계 데이터 번들 | index 청크 235KB — 해당 패키지 흔적 없음. 광복절·대체공휴일(8/17) 정상 렌더 | ✅ 해결 |
| **P0-2** 폰트 5MB TTF 7종 | **woff2 5종, 총 570KB** (KIMM 107 / Bold 102 / SemiBold 128 / ExtraBold 108 / Regular 125) | ✅ 해결 |
| **P1-7** 서비스워커에 캐싱 없음 | `navigator.serviceWorker.controller` = **true**, 재방문 시 load 완료 **95ms** | ✅ 해결 |
| **D6** 헤더 아이콘 4개 라벨 없음 | 검색 · 집중 **2개로 축소**, 나머지는 설정 탭으로 이동 | ✅ 해결 |
| **D12(일부)** 모달 Escape 미지원 | Escape로 정상 닫힘 확인 | ✅ 해결 |
| 콘솔 에러 | 0건 | ✅ 문제없음 |

**성능 실측 (재방문·SW 캐시 상태)**
```
TTFB            43ms
DOM Interactive 58ms
DOMContentLoaded 81ms
Load 완료       95ms
```
재방문 성능은 매우 좋습니다.

⚠️ 다만 **첫 방문(콜드) 시 FCP 4,372ms**를 기록했습니다. 폰트 5개(570KB)가 동시에 요청되는 구조라 첫 진입이 느릴 가능성이 큽니다. 실기기·3G 스로틀링으로 재측정을 권합니다. 남은 개선 여지는 **한글 서브셋**(2,350자 기준 폰트당 30~50KB까지 감소)과 **가중치 축소**입니다.

---

## 2. 라이브에서 새로 확인한 버그

### L1. 자녀 일정이 시간순으로 정렬되지 않음 ★
홈 화면 "자녀 일정" 카드 실제 출력:
```
연두  영어 · 오후 1:00 ~ 오후 2:00
연두  수학 · 오후 2:00 ~ 오후 3:00
초롱  수학 · 오후 4:00 ~ 오후 5:00   ← 4시가
초롱  영어 · 오후 2:00 ~ 오후 3:00   ← 2시보다 먼저
```
`HomeView`의 `childEvents`는 필터만 하고 정렬을 하지 않습니다. 같은 화면의 `ChildWeekView`는 `timeToMinutes`로 정렬하고 있어서 **같은 앱 안에서 정렬 여부가 다릅니다.** 한 줄 추가로 해결됩니다.

### L2. 홈의 "오늘 일정 4개"를 눌러 캘린더로 가면 0개 ★
- 홈 요약: **"오늘 일정 4개 · 마감 할 일 1개"** (자녀 일정 포함 = `eventsForDate`)
- 홈 "오늘의 일정" 카드: **"그 밖의 일정은 없습니다."**
- 캘린더 → 가족 모드 → 8/11 선택: **"등록된 일정이 없습니다"** (자녀 제외 = `generalEventsForDate`)

같은 날짜에 대해 세 곳이 4개 / 0개 / 0개를 말합니다. 자녀 일정을 "가족 캘린더"에서 빼는 것 자체는 설계 의도로 보이지만, **요약 카운트만 다른 기준을 쓰는 게 문제**입니다. 사용자는 4개가 있다고 해서 캘린더에 들어갔다가 빈 화면을 봅니다.

→ 홈 요약도 같은 기준으로 세거나, "자녀 4개"로 출처를 밝히거나, 가족 모드에서 자녀 일정을 흐린 색으로라도 표시.

### L3. 시간 표기 형식이 섞여 있음
근무 카드만 **24시간제**입니다.
```
근무 카드   :  "22:30 종료"
자녀 일정   :  "오후 1:00 ~ 오후 2:00"
일정 카드   :  "오전 9:00"
```
앱 전체가 한글 오전/오후를 쓰는데 근무 종료 시각만 `22:30`. 근무 유형 설정에서 입력한 값이 그대로 출력되는 것으로 보입니다.

### L4. 할 일 개수 불일치 — 앞선 F7이 라이브에서 재현
```
헤드라인 : "남은 할 일이 3개 있어요"
이번 주   : 긴급 1 · 장보기 1 · 집안일 0  → 미완료 2개
```
헤드라인은 전체 기준, 목록은 필터 기준이라 항상 어긋납니다.

추가로 **완료된 항목("교복 드라이 맡김")은 목록에 남아 있는데 카운트에선 빠집니다.** 카운트 배지가 0인 칸에 카드가 하나 보이는 상태라 더 혼란스럽습니다.

---

## 3. 라이브에서 그대로 확인된 기존 지적

| 항목 | 확인 내용 |
|---|---|
| **F1** 일반 일정 반복 없음 | 일정 추가 모달 실물 확인: 제목·기간·시간·장소·담당자뿐. **반복 필드 없음** |
| **F3** 담당자 1명 | 담당자 픽커가 단일 선택(가족/아빠/엄마/초롱/연두 중 하나) |
| **F4** 입력 중 충돌 경고 없음 | 모달에 경고 영역 자체가 없음 |
| **F6** "오늘로 이동" 없음 | `‹ 2026년 8월 ›` 화살표만 존재 |
| **D3** 빈 카드가 자리 차지 | "오늘의 일정" 카드가 "그 밖의 일정은 없습니다." 한 줄인데 카드 높이는 그대로 |
| **D4** 월 그리드 밀도 낮음 | 셀 높이 100px, 대부분 빈칸. 이벤트는 9~10px 텍스트 2줄까지만 |
| **D5** 점 표기 중복 | 8/1 셀에 점 2개 **+** "호주 출발 / 부산 출발" 텍스트 2줄이 동시에. 공휴일도 빨간 텍스트 + 점 중복 |
| **D9** 일정 관리 폼 우선 | 등록 폼이 화면을 가득 채우고 목록은 폴드 아래 (탭에 개수 배지가 붙은 건 좋음) |
| **D10** 데스크톱 여백 과다 | 할 일 화면에서 카드 3개 아래로 화면 절반이 빈 공간. 캘린더 우측 패널 아래도 동일 |
| **D12** 포커스 복귀 없음 | Escape로 닫은 뒤 `document.activeElement`가 `BODY` — 열었던 버튼으로 복귀하지 않음 |

**모바일 검증은 못 했습니다.** 창 크기 조절이 뷰포트에 반영되지 않아(`innerWidth`가 1440에서 안 바뀜) 모바일 레이아웃은 확인하지 못했습니다. D1(홈 폴드)·D2(캘린더 상세 패널)·D8(할 일 3단 스택)은 **실기기나 개발자도구 디바이스 모드로 직접 확인**해 주세요.

---

## 4. 잘 작동하는 것

- **공휴일 로직 정확** — 8/15 광복절(토) → 8/17 대체공휴일 정상 계산
- **동기화** — "가족 일정 동기화됨 · 마지막 동기화 8. 11. 오전 10:55" 정상 표시
- **일정 관리 탭 배지** — 자녀 일정 4 / 학기·방학 4 / 자녀 정보 2 / 기념일 5, 한눈에 파악됨
- **코드 스플리팅** — 패널 3종이 별도 청크로 분리됨
- **콘솔 에러 0건**

> 참고: 지연 로드된 패널 3개(`FamilySyncPanel`, `FamilySettingsPanel`, `EventCollaborationPanel`)가 초기 로드 91~123ms 구간에 **결국 다 받아집니다.** 청크 분리는 됐지만 실제로 지연되진 않고 있어서, 사용자가 열 때 로드되도록 트리거를 조정하면 초기 페이로드를 더 줄일 수 있습니다.

---

## 5. 갱신된 우선순위

**바로 고칠 것 (전부 한 줄~수십 줄)**
1. **L1** 자녀 일정 시간순 정렬 — `.sort((a,b)=>timeToMinutes(a.time)-timeToMinutes(b.time))`
2. **L3** 근무 종료 시각을 오전/오후 표기로 통일
3. **L4** 할 일 헤드라인을 필터 기준으로 계산 + 완료 항목 처리 결정
4. **L2** 홈 요약 카운트와 캘린더 기준 일치

**다음 단계**
5. **F1** 일반 일정 반복 — 기존 반복 인프라 재사용
6. **F2** 주간 뷰 / 홈 "이번 주" 스트립
7. 모바일 실기기 확인 후 D1·D2 대응

**성능 마무리**
8. 폰트 한글 서브셋 + 가중치 축소 → 콜드 FCP 개선
9. 패널 3종 실제 지연 로드
